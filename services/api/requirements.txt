# === API image (lightweight ~200MB) ===

# Web framework
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
pydantic>=2.7.0
gunicorn>=22.0.0

# Auth
python-jose[cryptography]>=3.3.0
passlib[bcrypt]>=1.7.4
python-multipart>=0.0.12

# Data / HTTP
httpx>=0.28.0
redis>=5.0.0

# Supabase — PINNED to avoid pyiceberg transitive dependency
# pyiceberg requires Cython + C libs which bloat Docker images
# and break RunPod worker "Wake GPU" startup times.
supabase>=2.3.0,<2.16.0

# Billing
stripe>=5.0.0

# Observability
structlog>=23.0.0
prometheus-client>=0.17.0

# AI (lightweight API client only)
openai>=1.0.0
tenacity>=8.2.0
tiktoken>=0.5.0

# Env
python-dotenv>=1.0.0
