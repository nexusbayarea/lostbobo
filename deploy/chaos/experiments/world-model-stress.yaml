apiVersion: chaos-mesh.org/v1alpha1
kind: StressChaos
metadata:
  name: world-model-stress
  namespace: simhpc
spec:
  mode: one
  selector:
    labelSelectors:
      app: simhpc-world-model
  stressors:
    memory:
      workers: 1
      size: "512MB"
  duration: "60s"
