[project]
name = "simhpc-backend"
version = "3.5.0"
description = "High-performance urban athletic culture platform"
dependencies = [
    "reportlab",
    "qrcode[pil]",
    "requests",
    "supervisor",
]

[tool.setuptools]
packages = ["app", "packages"]

[build-system]
requires = ["setuptools>=61.0", "wheel"]
build-backend = "setuptools.build_meta"
