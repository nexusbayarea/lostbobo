name: Deploy

on:
  workflow_run:
    workflows: ["Worker Build"]
    types:
      - completed

jobs:
  deploy:
    runs-on: ubuntu-latest
    if: github.event.workflow_run.conclusion == 'success'

    steps:
      - name: Deploy
        run: |
          echo "Deploying SHA ${{ github.event.workflow_run.head_sha }}"
          # Add your deploy command here