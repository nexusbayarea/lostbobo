name: frontend-ci

on: [push, pull_request]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Install frontend deps
        run: |
          cd frontend
          npm install

      - name: TypeScript typecheck (import safety gate)
        run: |
          cd frontend
          npm run typecheck

      - name: Resolve imports (filesystem gate)
        run: |
          cd frontend
          npm run resolve

      - name: Build frontend
        run: |
          cd frontend
          npm run build
