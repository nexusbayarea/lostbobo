name: SimHPC SaaS Orchestrator (v2.8.0)

on:
  push:
    branches: [main]

concurrency:
  group: simhpc-main
  cancel-in-progress: false

jobs:
  self-heal-and-deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Setup uv
        run: |
          curl -LsSf https://astral.sh/uv/install.sh | sh
          echo "$HOME/.local/bin" >> $GITHUB_PATH

      - name: Create venv and install ruff
        run: |
          uv venv
          uv pip install ruff

      - name: Self-Heal Codebase
        run: |
          uv run ruff check . --fix --select F401,I || true
          sed -i 's/import httpx/import httpx  # noqa: E402/g' app/services/worker/worker.py || true

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3
        with:
          driver: docker-container

      - name: Login to GHCR
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.repository_owner }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build & Push Unified Image
        uses: docker/build-push-action@v6
        with:
          context: .
          file: ./docker/images/Dockerfile.unified
          push: true
          tags: |
            ghcr.io/${{ github.repository_owner }}/simhpc-unified:latest
            ghcr.io/${{ github.repository_owner }}/simhpc-unified:${{ github.sha }}
          cache-from: type=gha
          cache-to: type=gha,mode=max

      - name: Reset RunPod Fleet
        env:
          RUNPOD_API_KEY: ${{ secrets.RUNPOD_API_KEY }}
          RUNPOD_ID: ${{ secrets.RUNPOD_ID }}
        run: |
          curl -s -X POST "https://api.runpod.io/graphql" \
            -H "Authorization: Bearer $RUNPOD_API_KEY" \
            -H "Content-Type: application/json" \
            -d "{\"query\": \"mutation { podStop(input: { podId: \\\"$RUNPOD_ID\\\" }) { id } }\"}"
          
          sleep 15

          curl -s -X POST "https://api.runpod.io/graphql" \
            -H "Authorization: Bearer $RUNPOD_API_KEY" \
            -H "Content-Type: application/json" \
            -d "{\"query\": \"mutation { podResume(input: { podId: \\\"$RUNPOD_ID\\\", gpuCount: 1 }) { id } }\"}"