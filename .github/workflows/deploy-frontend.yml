name: Deploy Frontend v2.7

on:
  push:
    branches: [main]
    paths:
      - 'frontend/**'
      - 'app/api/routes/**'

jobs:
  deploy-frontend:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      # 1. SCOPE GUARD (Prevents AI from tampering with infra)
      - name: Scope Check
        run: bash scripts/guard_frontend.sh

      # 2. FAST LINT (Using uv)
      - name: Install uv & Ruff
        run: |
          curl -LsSf https://astral.sh/uv/install.sh | sh
          uv pip install ruff
      
      - name: Lint API Routes
        run: uv run ruff check app/api/routes/

      # 3. DETERMINISTIC FRONTEND BUILD
      - name: Build React Cockpit
        working-directory: ./frontend
        run: |
          npm ci
          npm run build

      # 4. SECRET INJECTION & VERCEL DEPLOY
      - name: Infisical Push
        env:
          INFISICAL_TOKEN: ${{ secrets.INFISICAL_TOKEN }}
        run: |
          export INFISICAL_TOKEN=$INFISICAL_TOKEN
          infisical run --env=production -- vercel --prod --yes

      # 5. CROSS-PLANE HEALTH CHECK (Port 8080)
      - name: Validate System Alignment
        run: |
          echo "Verifying Frontend -> Backend Connectivity..."
          # Target the unified proxy port
          STATUS=$(curl -s -o /dev/null -w "%{http_code}" "https://${{ secrets.RUNPOD_ID }}-8080.proxy.runpod.net/health")
          if [ "$STATUS" != "200" ]; then
            echo "❌ Backend Port 8080 Unreachable. Aborting."
            exit 1
          fi
