apiVersion: v1
kind: Service
metadata:
  name: {{ include "simhpc.fullname" . }}
  labels:
    {{- include "simhpc.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: 8000
      protocol: TCP
      name: http
  selector:
    {{- include "simhpc.selectorLabels" . | nindent 4 }}
